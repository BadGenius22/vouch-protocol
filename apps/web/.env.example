# ===========================================
# Vouch Protocol - Environment Configuration
# ===========================================
# Copy to .env.local for local development
# For Vercel: Set these in Project Settings > Environment Variables

# ---------- Solana Network ----------
# devnet for development/staging, mainnet-beta for production
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# ---------- Program ID ----------
# Current devnet deployment
NEXT_PUBLIC_VERIFIER_PROGRAM_ID=AgG8EAwpeWqjoJQBtUY5SHX38gYpsVhcG4x7UuRHwxk7

# ---------- Verifier Keypair (SERVER-SIDE ONLY) ----------
# CRITICAL for production! The verifier signs attestations.
# This keypair must be registered in the Solana program.
#
# Generate with: solana-keygen new --no-bip39-passphrase -o verifier-keypair.json
# Then copy the JSON array: cat verifier-keypair.json
#
# Format: JSON array [n1,n2,n3,...,n64] (64 bytes)
#
# WARNING: If not set, a random keypair is generated on each deploy,
# which will NOT match the registered verifier on-chain!
#
# VERIFIER_PRIVATE_KEY=[1,2,3,...64 bytes]

# ---------- Helius API (SERVER-SIDE ONLY) ----------
# Optional - fetches real on-chain data for proof generation
# Get free key at: https://dev.helius.xyz/
# Falls back to mock data if not set
#
# HELIUS_API_KEY=your_helius_api_key

# ---------- Feature Flags ----------
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_DEBUG=true

# ---------- Optional: Better RPC (for production) ----------
# Helius provides devnet RPC too
# NEXT_PUBLIC_SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_KEY
