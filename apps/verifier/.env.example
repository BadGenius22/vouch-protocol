# Vouch Verifier Service Configuration

# Server port
PORT=3001

# CORS origins (comma-separated)
CORS_ORIGIN=http://localhost:3000

# Verifier private key (base58 or JSON array format)
# Generate with: solana-keygen new --no-bip39-passphrase
# For production, this key must be registered in the Anchor program
# VERIFIER_PRIVATE_KEY=

# Path to circuit artifacts (defaults to ../web/public/circuits)
# CIRCUITS_DIR=
